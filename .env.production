# Production Environment
# Note: Console logging is completely disabled in production mode (isDevelopment check)
# This setting is overridden by the logger which checks import.meta.env.MODE === 'production'
VITE_LOG_LEVEL=silent
VITE_DEBUG=false
